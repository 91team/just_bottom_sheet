name: just_bottom_sheet
description: Flexible bottom sheet widget, that does nothing redundant
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic91:
    git:
      url: https://github.com/91team/pedantic91.git
      ref: v0.3.6
